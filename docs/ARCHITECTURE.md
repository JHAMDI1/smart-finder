# 🏛️ Architecture Overview - Smart Finder

Vue d'ensemble complète de l'architecture du projet Smart Finder.

---

## 📊 System Context Diagram (C4 - Level 1)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                          SMART FINDER SYSTEM                            │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│   ┌─────────────┐      ┌─────────────────┐      ┌──────────────┐     │
│   │  Travailleur │      │                 │      │   Smart      │     │
│   │  / Étudiant  │─────►│  Smart Finder   │      │   Finder     │     │
│   │  (Recherche) │      │  Application    │◄─────│   Admin      │     │
│   └─────────────┘      │                 │      └──────────────┘     │
│                        │                 │                            │
│   ┌─────────────┐      │  ┌─────────┐   │                            │
│   │  Propriétaire│─────►│  │         │   │                            │
│   │  (Gérant)    │      │  │   IA    │   │                            │
│   └─────────────┘      │  │ Module  │   │                            │
│                        │  └─────────┘   │                            │
│                        └─────────────────┘                            │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🏗️ Container Diagram (C4 - Level 2)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           SMART FINDER                                   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────────┐         ┌─────────────────┐         ┌────────────┐ │
│  │   WEB APP       │         │   API           │         │  DATABASE  │ │
│  │   (Angular)     │◄───────►│   (Spring Boot) │◄───────►│  (MySQL)   │ │
│  │                 │  HTTPS  │                 │  JDBC   │            │ │
│  │ • Components    │         │ • Controllers   │         │ • Lieux    │ │
│  │ • Services      │         │ • Services      │         │ • Critères │ │
│  │ • Guards        │         │ • Repositories  │         │ • Avis     │ │
│  │ • Interceptors  │         │ • Specifications│         │ • Users    │ │
│  └─────────────────┘         └────────┬────────┘         └────────────┘ │
│                                      │                                  │
│                                      ▼                                  │
│                            ┌─────────────────┐                          │
│                            │   LLM Service   │                          │
│                            │  (OpenAI/Claude)│                          │
│                            │                 │                          │
│                            │ • Smart Search  │                          │
│                            │ • Extraction    │                          │
│                            └─────────────────┘                          │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🧩 Component Diagram - Backend (C4 - Level 3)

```
┌─────────────────────────────────────────────────────────────────────────┐
│                       SPRING BOOT APPLICATION                            │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                         LAYER: CONTROLLERS                       │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐         │   │
│  │  │AuthCtrl  │ │LieuCtrl  │ │CritereCtrl│ │AvisCtrl  │         │   │
│  │  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘         │   │
│  └───────┼────────────┼────────────┼────────────┼───────────────┘   │
│          │            │            │            │                     │
│          ▼            ▼            ▼            ▼                     │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                         LAYER: SERVICES                          │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐         │   │
│  │  │AuthSvc   │ │LieuSvc   │ │CritereSvc│ │AvisSvc   │         │   │
│  │  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘         │   │
│  │       │            │            │            │                  │   │
│  │       └────────────┴─────┬──────┴────────────┘                  │   │
│  │                            │                                     │   │
│  │                            ▼                                     │   │
│  │                  ┌─────────────────┐                             │   │
│  │                  │ SmartSearchSvc  │                             │   │
│  │                  │ (Module IA)     │                             │   │
│  │                  └────────┬────────┘                             │   │
│  └───────────────────────────┼─────────────────────────────────────┘   │
│                              │                                         │
│                              ▼                                         │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                      LAYER: REPOSITORIES                         │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐         │   │
│  │  │UserRepo  │ │LieuRepo  │ │CritereRepo│ │AvisRepo  │         │   │
│  │  │(JPA)     │ │(JPA+Spec)│ │(JPA)     │ │(JPA)     │         │   │
│  │  └────┬─────┘ └────┬─────┘ └────┬─────┘ └────┬─────┘         │   │
│  └───────┼────────────┼────────────┼────────────┼───────────────┘   │
│          │            │            │            │                     │
│          ▼            ▼            ▼            ▼                     │
│  ┌─────────────────────────────────────────────────────────────────┐   │
│  │                      LAYER: INFRASTRUCTURE                         │   │
│  │  ┌──────────┐ ┌──────────┐ ┌──────────┐ ┌──────────┐         │   │
│  │  │MySQL     │ │LLM Client│ │JWT Utils │ │Validators│         │   │
│  │  └──────────┘ └──────────┘ └──────────┘ └──────────┘         │   │
│  └─────────────────────────────────────────────────────────────────┘   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 📱 Frontend Architecture

### Module Structure

```
src/app/
│
├── core/                          # Singletons, global services
│   ├── guards/
│   │   ├── auth.guard.ts          # Protection routes auth
│   │   └── role.guard.ts          # Protection par rôle
│   ├── interceptors/
│   │   ├── jwt.interceptor.ts     # Ajout token JWT
│   │   └── error.interceptor.ts   # Gestion erreurs HTTP
│   └── services/
│       ├── auth.service.ts
│       └── api.service.ts
│
├── shared/                        # Composants réutilisables
│   ├── components/
│   │   ├── star-rating/
│   │   ├── loading-spinner/
│   │   └── error-message/
│   └── pipes/
│
└── features/                      # Modules par feature (lazy-loaded)
    ├── auth/
    │   ├── login/
    │   ├── register/
    │   └── auth.module.ts
    │
    ├── lieux/
    │   ├── lieu-list/
    │   ├── lieu-card/
    │   ├── lieu-detail/
    │   ├── lieu-filters/
    │   ├── lieu.service.ts
    │   └── lieux.module.ts
    │
    ├── search/
    │   ├── smart-search/
    │   └── search.module.ts
    │
    └── admin/
        └── admin.module.ts
```

---

## 🗄️ Data Architecture

### Entity Relationships

```
┌─────────────────────────────────────────────────────────────────────────┐
│                       DATABASE SCHEMA                                   │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────────┐       ┌─────────────────┐       ┌──────────────┐     │
│  │  Utilisateur │       │      Lieu       │       │   Critere    │     │
│  │──────────────│       │─────────────────│       │──────────────│     │
│  │ PK: id       │◄──────┤ PK: id          │◄──────┤ PK: id       │     │
│  │ email (UQ)   │  1:N  │ nom             │  N:M  │ nom (UQ)     │     │
│  │ password     │       │ adresse         │       │ categorie    │     │
│  │ role         │       │ lat/long        │       └──────┬───────┘     │
│  └──────────────┘       │ proprietaire_id │              │             │
│                         │ note_moyenne    │              │             │
│                         └────────┬────────┘              │             │
│                                  │                      │             │
│                         ┌────────┴────────┐            │             │
│                         │      Avis       │            │             │
│                         │─────────────────│            │             │
│                         │ PK: id          │            │             │
│                         │ FK: lieu_id     │            │             │
│                         │ FK: user_id     │            │             │
│                         │ note (1-5)      │            │             │
│                         │ commentaire     │            │             │
│                         └─────────────────┘            │             │
│                                                        │             │
│                                               ┌────────┴────────┐    │
│                                               │  lieu_critere   │    │
│                                               │  (join table)   │    │
│                                               │─────────────────│    │
│                                               │ PK: (lieu, crit)│    │
│                                               │ FK: lieu_id     │    │
│                                               │ FK: critere_id  │    │
│                                               └─────────────────┘    │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🧠 Smart Search Architecture (Module IA)

### Component Flow

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    SMART SEARCH FLOW                                     │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  1. INPUT: "café calme avec wifi ouvert le dimanche"                  │
│                                                                         │
│  2. PROMPT BUILDER                                                      │
│     ┌─────────────────────────────────────────────────────────────┐    │
│     │ Template: prompts/search-extraction.txt                      │    │
│     │ Variables:                                                   │    │
│     │   - userQuery                                                │    │
│     │   - availableCriteria (from DB cache)                       │    │
│     │   - examples (few-shot learning)                            │    │
│     └─────────────────────────────────────────────────────────────┘    │
│                                                                         │
│  3. LLM SERVICE (OpenAI/Claude)                                       │
│     ┌─────────────────────────────────────────────────────────────┐    │
│     │ • API Call with timeout (5s)                               │    │
│     │ • Retry logic (3 attempts)                                 │    │
│     │ • Circuit breaker pattern                                    │    │
│     └─────────────────────────────────────────────────────────────┘    │
│                                                                         │
│  4. RESPONSE PARSER                                                     │
│     Expected JSON:                                                      │
│     {                                                                   │
│       "tags": ["wifi", "calme", "dimanche"],                           │
│       "confidence": 0.95,                                              │
│       "explanation": "...",                                             │
│       "unknownCriteria": []                                             │
│     }                                                                   │
│                                                                         │
│  5. TAG MAPPER                                                          │
│     Map "wifi" → 1L                                                    │
│     Map "calme" → 5L                                                   │
│     Map "dimanche" → 12L                                               │
│                                                                         │
│  6. SEARCH EXECUTION                                                    │
│     Call LieuSearchService with mapped IDs                            │
│     Apply standard ranking algorithm                                  │
│                                                                         │
│  7. RESPONSE                                                            │
│     {                                                                   │
│       "query": "café calme...",                                         │
│       "understood": { "tags": [...], "explanation": "..." },           │
│       "results": [...],                                                 │
│       "fallback": false                                                 │
│     }                                                                   │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🔐 Security Architecture

### JWT Authentication Flow

```
┌─────────────┐         ┌─────────────┐         ┌─────────────┐
│   Client    │         │   Backend   │         │   MySQL     │
│  (Angular)  │         │  (Spring)   │         │             │
└──────┬──────┘         └──────┬──────┘         └─────────────┘
       │                         │
       │  1. POST /auth/login    │
       │ ──────────────────────► │
       │  {email, password}      │
       │                         │
       │  2. Validate credentials│
       │                         ├──► Verify hash
       │                         │
       │  3. Generate JWT        │
       │ ◄────────────────────── │
       │  {token, user}          │
       │                         │
       │  4. Store in localStorage│
       │                         │
       │  5. API calls with      │
       │     Authorization:      │
       │     Bearer <token>      │
       │ ──────────────────────► │
       │                         │
       │  6. Validate JWT        │
       │    (JwtAuthFilter)      │
       │                         │
       │  7. Check permissions   │
       │    (@PreAuthorize)      │
       │                         │
       │  8. Return data         │
       │ ◄────────────────────── │
       │                         │
```

### 6.2 CORS Configuration (CRITIQUE)

**⚠️ IMPORTANT** : Jamais `allowedOrigins("*")` avec `allowCredentials(true)`

```java
@Bean
public CorsConfigurationSource corsConfigurationSource() {
    CorsConfiguration config = new CorsConfiguration();
    // ✅ Origines EXPLICITES uniquement
    config.setAllowedOrigins(List.of(
        "http://localhost:4200",           // Dev
        "https://smart-finder.vercel.app"  // Prod
    ));
    config.setAllowedMethods(List.of("GET", "POST", "PUT", "DELETE", "OPTIONS"));
    config.setAllowedHeaders(List.of("*"));
    config.setAllowCredentials(true);  // OK car origines explicites
    
    UrlBasedCorsConfigurationSource source = new UrlBasedCorsConfigurationSource();
    source.registerCorsConfiguration("/**", config);
    return source;
}
```

---

## 📦 Deployment Architecture

### Production Setup

```
┌─────────────────────────────────────────────────────────────────────────┐
│                         PRODUCTION                                       │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────────┐      ┌──────────────┐      ┌──────────────┐          │
│  │   Vercel     │      │   Railway    │      │ PlanetScale  │          │
│  │  (Frontend)  │◄────►│  (Backend)   │◄────►│  (MySQL)     │          │
│  │   Angular    │ HTTPS│ Spring Boot  │      │              │          │
│  └──────────────┘      └──────┬───────┘      └──────────────┘          │
│                               │                                        │
│                               ▼                                        │
│                        ┌──────────────┐                                 │
│                        │   Redis      │                                 │
│                        │   (Cache)    │                                 │
│                        └──────────────┘                                 │
│                                                                         │
│  ┌──────────────┐                                                       │
│  │   OpenAI     │                                                       │
│  │   API        │                                                       │
│  └──────────────┘                                                       │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

---

## 🎯 Architecture Principles

### 1. **Feature-Based Organization**
- Code organisé par fonctionnalité plutôt que par type technique
- Chaque feature contient ses propres controllers, services, repositories
- Facilite la maintenance et le scaling

### 2. **Separation of Concerns**
- Controllers: HTTP handling only
- Services: Business logic
- Repositories: Data access
- DTOs: API contracts

### 3. **API-First Design**
- API RESTful bien définie
- Documentation OpenAPI/Swagger
- Versioning (/api/v1/)

### 4. **Resilience Patterns**
- Circuit breaker pour LLM API
- Fallbacks pour les erreurs
- Retry avec backoff

### 5. **Security by Design**
- JWT stateless authentication
- CORS configuré explicitement
- Input validation
- SQL injection prevention (JPA)

---

## 📊 Technology Stack Summary

| Layer | Technology | Version | Purpose |
|-------|-----------|---------|---------|
| Frontend | Angular | 17+ | SPA Framework |
| Frontend | TypeScript | 5+ | Type safety |
| Frontend | Tailwind CSS | 3+ | Styling |
| Frontend | RxJS | 7+ | Reactive programming |
| Backend | Spring Boot | 3.2+ | Java framework |
| Backend | Spring Data JPA | 3.2+ | ORM |
| Backend | Spring Security | 3.2+ | Security |
| Backend | JWT | 0.12+ | Authentication |
| Database | MySQL | 8.0+ | Relational DB |
| Database | Flyway | 10+ | Migrations |
| AI | OpenAI API | v1 | LLM integration |
| Cache | Redis | 7+ | Caching (optional) |
| Build | Maven | 3.9+ | Java build |
| Build | Angular CLI | 17+ | Frontend build |

---

## 🔄 Data Flow Examples

### Search Flow

```
User (Angular) 
    ↓ POST /api/v1/search/lieux
Controller (SearchController)
    ↓
Service (LieuSearchService)
    ↓
Specifications (LieuSpecifications)
    ↓
Repository (LieuRepository)
    ↓
Database (MySQL)
    ↓
Results → DTOs → JSON Response
```

### Smart Search Flow

```
User Input (Angular)
    ↓ POST /api/v1/smart-search
Controller (SmartSearchController)
    ↓
Service (SmartSearchService)
    ↓
LLM Service → OpenAI API
    ↓
Parse Response (tags)
    ↓
Map to IDs
    ↓
Execute Standard Search
    ↓
Results + Explanation
```

---

## 📚 Architecture Decision Records (ADRs)

Les décisions architecturales importantes sont documentées dans `/docs/adr/`:

1. **[ADR-001](./adr/ADR-001-jpa-specifications.md)** : Choix de JPA Specifications
2. **[ADR-002](./adr/ADR-002-feature-based-packages.md)** : Structure feature-based
3. **[ADR-003](./adr/ADR-003-jwt-security.md)** : Sécurité JWT

---

## 🎯 Next Steps

Pour implémenter cette architecture :

1. **[Backend Guide](../BACKEND_GUIDE.md)** - Commencer le développement backend
2. **[Frontend Guide](../FRONTEND_GUIDE.md)** - Développer le frontend
3. **[Database Guide](../DATABASE.md)** - Configurer la base de données
4. **[API Reference](../API_REFERENCE.md)** - Intégrer les endpoints

---

**Version** : 1.0  
**Last Updated** : Février 2026
